I'm expanding my knowledge from React Vite to React Native with this project. This will allow me to get started developing iPhone apps. It's a gentle transition if you know JavaScript and TypeScript.

This is RnD for combining mathmatics with UX. It's an early attempt and this subject will need many revisions with less spaghetti as I get more familiar with elements of mathmatics, programming and UX.

Font used: Faculty Glyphic borrowed from Google Fonts.


```zsh
npm install uuid
```

```zsh
npm install react-native-uuid
```

Future Features and ambitions:
---
Flexible options other than `null, a, b, c, x, y, x`:
I wanted to do a dynamic import of variables from `MagScrollLibraryData.json`. Essentially, the options (null, a, b, c, x, y, z) would function dynamically with the json table across the entire app. At my current skil level (as of writing this) the research it would require would stop me from doing progress, so I will have to let the options remain hardcoded until a future reiteration when I'm familiar with dynamic useStates.